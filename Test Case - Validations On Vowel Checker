
#unit test.cs

using System;
using System.Text;
using NUnit.Framework;
namespace VowelChecker
{
    // 1) Added the required test attribute for the class UnitTest
    [TestFixture]
    public class UnitTest
    {
        // 2) Added the required test attribute for each test method
        [Test]
        public void Test_CheckVowels_Valid()
        {
            // Arrange: Setup a valid vowel
            char input = 'a';
            bool expected = true;
            // Act: Call the static method from the Program class
            bool? actual = Program.CheckVowels(input);
            // 3) & 4) Use only one Assert and check if actual is equal to expected
            Assert.AreEqual(expected, actual);
        }
        [Test]
        public void Test_CheckVowels_Invalid()
        {
            // Arrange: Setup an invalid non-vowel character
            char input = 'z';
            bool expected = false;
            // Act: Call the static method
            bool? actual = Program.CheckVowels(input);
            // 3) & 4) Use only one Assert and check if actual is equal to expected
            Assert.AreEqual(expected, actual);
        }
    }
}
